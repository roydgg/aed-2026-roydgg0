# Explicación — Actividad 9

Completa este archivo con tus propias palabras.

1. Diferencia principal entre la solución iterativa y la recursiva: el iterativo utiliza un ciclo for para repetir los procesos y el recursivo hace que la funcion se llame a si misma hasta llegar al caso base

2. Caso base de `sumaRecursiva` y por qué es necesario: el caso base es if (n==0) {return 0;}, es necesario para que se detengan las llamadas y que no se llene la memoria.

3. Condición que hace terminar a `sumaIterativa`: termina cuando "i" llega a 0 i > 0

4. SHA del commit final entregado en Classroom:
